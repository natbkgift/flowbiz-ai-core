## 🧭 Codex Pre-Flight (Required)

### Persona (pick one)
- [x] persona:core
- [ ] persona:infra
- [ ] persona:docs

### Scope Lock 🔒
- **Goal (one sentence):** Introduce a deterministic prompt template system for rendering named templates with strict variable validation.
- **In-scope files:** `packages/core/contracts/prompt_template.py`, `packages/core/prompt_templates.py`, `packages/core/contracts/__init__.py`, `tests/test_prompt_templates.py`, `tests/test_contract_package.py`, `docs/pr_notes/PR-028.2.md`, `docs/PR_LOG.md`, `docs/PR_STATE.md`
- **Out-of-scope (3 bullets):**
  1. No LLM provider integration or runtime orchestration wiring
  2. No infra/deploy/VPS/nginx/.github changes
  3. No client/platform prompt business logic
- **Behavior changes:**
  - [ ] none
  - [x] yes (describe): Adds reusable prompt template registry/rendering primitives in core.

### Evidence (required)
- **PR description read:** [x]
- **CI failures read (if any):** [x]
- **Relevant logs / stack trace captured:** `Baseline on main before branch: ruff check . => PASS, pytest -q => 216 passed`

### Plan (max 5 steps)
1. Add schema contracts for prompt templates and render requests/results
2. Add deterministic in-memory template registry and renderer
3. Add tests for registration/render validation and error cases
4. Run `ruff check .` and `pytest -q`
5. Update PR docs/state and merge ff-only

### Commands I will run locally
- [x] `ruff check .`
- [x] `pytest -q`
- [ ] Any smoke command (if relevant): N/A

### Safety
- **New deps added?**
  - [x] no
  - [ ] yes — list + reason:
- **Secrets/env touched?**
  - [x] no
  - [ ] yes — explain:

### Exit Criteria
- [x] CI green on PR
- [x] Summary + Testing filled in PR body
- [x] No scope creep
