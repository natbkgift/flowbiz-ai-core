## 🧭 Codex Pre-Flight (Required)

### Persona (pick one)
- [x] persona:core
- [ ] persona:infra
- [ ] persona:docs

### Scope Lock 🔒
- **Goal (one sentence):** Add seed template manifest contracts and example template stubs/docs for reusable core-compatible project seeds.
- **In-scope files:** `packages/core/contracts/devx.py`, `tests/test_devx_contracts.py`, `docs/SEED_TEMPLATES.md`, `docs/contracts/stubs/seed_templates/*`, `docs/PR_LOG.md`, `docs/PR_STATE.md`, `docs/pr_notes/PR-103.md`
- **Out-of-scope (3 bullets):**
  1. No generated client projects or platform-specific scaffolding executors
  2. No UI/docs-site/playground implementation
  3. No infra/deploy changes (`.github`, `compose`, `nginx`, VPS)
- **Behavior changes:**
  - [ ] none
  - [x] yes (describe): Adds manifest/schema contracts and examples for seed templates only

### Evidence (required)
- **PR description read:** [x]
- **CI failures read (if any):** [x] — N/A
- **Relevant logs / stack trace captured:** `N/A`

### Plan (max 5 steps)
1. Extend `devx` contracts with seed template manifest models
2. Add/extend tests for manifest validation and helper behavior
3. Add docs and example template stubs under `docs/contracts/stubs/seed_templates/`
4. Run `ruff check .` and `pytest -q`
5. Update `PR_LOG` + `PR_STATE`, commit/merge `PR-103`

### Commands I will run locally
- [x] `ruff check .`
- [x] `pytest -q` (or `pytest tests/<specific> -q`)
- [ ] Any smoke command (if relevant): `N/A`

### Safety
- **New deps added?**
  - [x] no
  - [ ] yes — list + reason:
- **Secrets/env touched?**
  - [x] no
  - [ ] yes — explain:

### Exit Criteria
- [ ] CI green on PR
- [ ] Summary + Testing filled in PR body
- [x] No scope creep
