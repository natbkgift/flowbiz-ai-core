## 🧭 Codex Pre-Flight (Required)

### Persona (pick one)
- [x] persona:core
- [ ] persona:infra
- [ ] persona:docs

### Scope Lock 🔒
- **Goal (one sentence):** Add transport-agnostic LLM adapter abstraction contracts and in-memory stub implementation for future integrations.
- **In-scope files:** `packages/core/contracts/llm_adapter.py`, `packages/core/llm_adapter.py`, `packages/core/contracts/__init__.py`, `tests/test_llm_adapter.py`, `tests/test_contract_package.py`, `docs/pr_notes/PR-028.1.md`, `docs/PR_LOG.md`, `docs/PR_STATE.md`
- **Out-of-scope (3 bullets):**
  1. No external LLM SDK/provider integration
  2. No infra/deploy/VPS/nginx/.github changes
  3. No secrets/api-key handling in this PR
- **Behavior changes:**
  - [ ] none
  - [x] yes (describe): Introduces reusable LLM adapter interface + deterministic stub adapter; no runtime routing integration yet.

### Evidence (required)
- **PR description read:** [x]
- **CI failures read (if any):** [x]
- **Relevant logs / stack trace captured:** `Baseline on main before branch: ruff check . => PASS, pytest -q => 212 passed`

### Plan (max 5 steps)
1. Add schema-only contracts for LLM request/response and adapter metadata
2. Add adapter protocol and deterministic stub implementation
3. Add tests for validation and stub behavior
4. Run `ruff check .` and `pytest -q`
5. Update PR docs/state and merge ff-only

### Commands I will run locally
- [x] `ruff check .`
- [x] `pytest -q`
- [ ] Any smoke command (if relevant): N/A

### Safety
- **New deps added?**
  - [x] no
  - [ ] yes — list + reason:
- **Secrets/env touched?**
  - [x] no
  - [ ] yes — explain:

### Exit Criteria
- [x] CI green on PR
- [x] Summary + Testing filled in PR body
- [x] No scope creep
