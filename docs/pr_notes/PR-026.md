## 🧭 Codex Pre-Flight (Required)

### Persona (pick one)
- [x] persona:core
- [ ] persona:infra
- [ ] persona:docs

### Scope Lock 🔒
- **Goal (one sentence):** Add canonical response contract schemas for agent and tool envelopes with normalized error payloads.
- **In-scope files:** `packages/core/contracts/response.py`, `packages/core/contracts/__init__.py`, `tests/test_response_contracts.py`, `docs/PR_LOG.md`, `docs/PR_STATE.md`, `docs/pr_notes/PR-026.md`
- **Out-of-scope (3 bullets):**
  1. No endpoint behavior changes or API route redesign
  2. No infra/deploy/VPS/nginx/.github modifications
  3. No platform/client integration implementation
- **Behavior changes:**
  - [ ] none
  - [x] yes (describe): Introduces reusable response-envelope schemas for future runtime/tool integration.

### Evidence (required)
- **PR description read:** [x]
- **CI failures read (if any):** [x]
- **Relevant logs / stack trace captured:** `Baseline on main before branch: ruff check . => PASS, pytest -q => 200 passed`

### Plan (max 5 steps)
1. Add schema-only response contracts for error/agent/tool envelopes
2. Export contracts through package init
3. Add validation and serialization tests
4. Run `ruff check .` and `pytest -q`
5. Update PR docs/state and merge ff-only

### Commands I will run locally
- [x] `ruff check .`
- [x] `pytest -q`
- [ ] Any smoke command (if relevant): N/A

### Safety
- **New deps added?**
  - [x] no
  - [ ] yes — list + reason:
- **Secrets/env touched?**
  - [x] no
  - [ ] yes — explain:

### Exit Criteria
- [x] CI green on PR
- [x] Summary + Testing filled in PR body
- [x] No scope creep
