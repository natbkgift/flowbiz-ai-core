## 🧭 Codex Pre-Flight (Required)

### Persona (pick one)
- [ ] persona:core
- [ ] persona:infra
- [x] persona:docs

### Scope Lock 🔒
- **Goal (one sentence):** Document web app shell requirements/boundaries only, without implementing UI in core.
- **In-scope files:** `docs/WEB_APP_SHELL_SPEC.md`, `docs/PR_LOG.md`, `docs/PR_STATE.md`, `docs/pr_notes/PR-117.md`
- **Out-of-scope (3 bullets):**
  1. No frontend/web app shell code
  2. No auth/session/UI routing implementation
  3. No deploy/hosting config changes
- **Behavior changes:**
  - [ ] none
  - [x] yes (describe): Adds docs-only web app shell specification for external implementation

### Evidence (required)
- **PR description read:** [x]
- **CI failures read (if any):** [x] — N/A
- **Relevant logs / stack trace captured:** `N/A`

### Plan (max 5 steps)
1. Add `docs/WEB_APP_SHELL_SPEC.md`
2. Update PR tracking docs
3. Run `ruff check .` and `pytest -q`
4. Commit `PR-117`
5. Merge ff-only to `main`

### Commands I will run locally
- [x] `ruff check .`
- [x] `pytest -q` (or `pytest tests/<specific> -q`)
- [ ] Any smoke command (if relevant): `N/A`

### Safety
- **New deps added?**
  - [x] no
  - [ ] yes — list + reason:
- **Secrets/env touched?**
  - [x] no
  - [ ] yes — explain:

### Exit Criteria
- [ ] CI green on PR
- [ ] Summary + Testing filled in PR body
- [x] No scope creep
