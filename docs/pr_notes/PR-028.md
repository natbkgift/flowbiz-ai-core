## 🧭 Codex Pre-Flight (Required)

### Persona (pick one)
- [x] persona:core
- [ ] persona:infra
- [ ] persona:docs

### Scope Lock 🔒
- **Goal (one sentence):** Add an optional safety gate hook contract and runtime integration point with default allow behavior.
- **In-scope files:** `packages/core/contracts/safety.py`, `packages/core/safety_gate.py`, `packages/core/runtime/runtime.py`, `packages/core/contracts/__init__.py`, `tests/test_safety_gate.py`, `tests/test_runtime_unit.py`, `docs/pr_notes/PR-028.md`, `docs/PR_LOG.md`, `docs/PR_STATE.md`
- **Out-of-scope (3 bullets):**
  1. No infra/deploy/VPS/nginx/.github changes
  2. No external moderation provider integration
  3. No platform/client-specific enforcement policies
- **Behavior changes:**
  - [ ] none
  - [x] yes (describe): Runtime supports optional pre-execution safety hook; default behavior remains allow.

### Evidence (required)
- **PR description read:** [x]
- **CI failures read (if any):** [x]
- **Relevant logs / stack trace captured:** `Baseline on main: ruff check . => PASS, pytest -q => 209 passed`

### Plan (max 5 steps)
1. Add schema-only safety gate contracts
2. Implement default/no-op safety gate helper and protocol
3. Integrate optional safety check into runtime run pipeline
4. Add tests for allow/deny safety behavior
5. Run checks and update PR docs/state

### Commands I will run locally
- [x] `ruff check .`
- [x] `pytest -q`
- [ ] Any smoke command (if relevant): N/A

### Safety
- **New deps added?**
  - [x] no
  - [ ] yes — list + reason:
- **Secrets/env touched?**
  - [x] no
  - [ ] yes — explain:

### Exit Criteria
- [x] CI green on PR
- [x] Summary + Testing filled in PR body
- [x] No scope creep
