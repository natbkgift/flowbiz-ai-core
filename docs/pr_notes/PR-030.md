# PR-030 Preflight Checklist

## Metadata
- **PR ID:** PR-030
- **Title:** Routing rules v1 (rule-based intent router)
- **Persona:** persona:core
- **Scope class:** in-scope
- **In-scope files:** `packages/core/contracts/routing.py`, `packages/core/intent_router.py`, `packages/core/contracts/__init__.py`, `tests/test_intent_router.py`, `tests/test_contract_package.py`, `docs/PR_LOG.md`, `docs/PR_STATE.md`, `docs/pr_notes/PR-030.md`

## Scope Checklist
- [x] All touched files are in `packages/core/`, `tests/`, or `docs/`
- [x] No UI, billing, or platform-specific code
- [x] No new third-party dependencies
- [x] Contracts are frozen Pydantic models with `extra="forbid"`

## Implementation
1. Define `RoutingRule` and `RoutingResult` contracts in `packages/core/contracts/routing.py`
2. Implement `IntentRouter` with keyword + regex matching, priority ordering
3. Export new schemas from `packages/core/contracts/__init__.py`
4. Add 16 tests in `tests/test_intent_router.py`
5. Update `tests/test_contract_package.py` expected symbols
6. Update `docs/PR_LOG.md` and `docs/PR_STATE.md`

## Quality Gates
- [x] `ruff check .` — PASS
- [x] `pytest -q` — 256 passed
