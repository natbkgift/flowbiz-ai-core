## 🧭 Codex Pre-Flight (Required)

### Persona (pick one)
- [x] persona:core
- [ ] persona:infra
- [ ] persona:docs

### Scope Lock 🔒
- **Goal (one sentence):** Add a core CLI tool for local developer workflows (version/metadata/registry inspection) without introducing platform-specific integrations.
- **In-scope files:** `pyproject.toml`, `packages/core/cli.py`, `tests/test_cli.py`, `docs/PR_LOG.md`, `docs/PR_STATE.md`, `docs/pr_notes/PR-101.md`
- **Out-of-scope (3 bullets):**
  1. No changes to `.github/**`, `docker-compose*.yml`, `nginx/**`, or deployment/VPS config
  2. No UI/frontend, billing/payment, or platform adapter implementations
  3. No production API behavior changes in `apps/api/**` unless required for CLI-only local introspection (not expected)
- **Behavior changes:**
  - [ ] none
  - [x] yes (describe): Adds a new CLI entry point/command surface for local inspection of core metadata and registry state

### Evidence (required)
- **PR description read:** [x]
- **CI failures read (if any):** [x] — N/A (no active CI failure context for local PR implementation)
- **Relevant logs / stack trace captured:** `N/A`

### Plan (max 5 steps)
1. Confirm baseline (`git status`, `ruff check .`, `pytest -q`) before starting PR-101
2. Implement CLI module and console script entrypoint using stdlib `argparse`
3. Add focused tests for CLI commands and outputs
4. Run `ruff check .` and `pytest -q`
5. Update `docs/PR_LOG.md` + `docs/PR_STATE.md`, commit `PR-101`, and merge ff-only to `main`

### Commands I will run locally
- [x] `ruff check .`
- [x] `pytest -q` (or `pytest tests/<specific> -q`)
- [ ] Any smoke command (if relevant): `python -m packages.core.cli --help` (optional local smoke)

### Safety
- **New deps added?**
  - [x] no
  - [ ] yes — list + reason:
- **Secrets/env touched?**
  - [x] no
  - [ ] yes — explain:

### Exit Criteria
- [ ] CI green on PR
- [ ] Summary + Testing filled in PR body
- [x] No scope creep
