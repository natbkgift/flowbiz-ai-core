## 🧭 Codex Pre-Flight (Required)

### Persona (pick one)
- [x] persona:core
- [ ] persona:infra
- [ ] persona:docs

### Scope Lock 🔒
- **Goal (one sentence):** Add WhatsApp connector contracts/stubs and docs only in core (no real integration).
- **In-scope files:** `packages/core/contracts/integrations.py`, `tests/test_integrations_contracts.py`, `docs/WHATSAPP_CONNECTOR_SPEC.md`, `docs/contracts/stubs/integrations/whatsapp_event.json`, `docs/PR_LOG.md`, `docs/PR_STATE.md`, `docs/pr_notes/PR-113.md`
- **Out-of-scope (3 bullets):**
  1. No WhatsApp API/webhook implementation
  2. No secrets/provider credentials/runtime callbacks
  3. No infra/deploy changes
- **Behavior changes:**
  - [ ] none
  - [x] yes (describe): Adds WhatsApp integration boundary contracts and docs/stubs only

### Evidence (required)
- **PR description read:** [x]
- **CI failures read (if any):** [x] — N/A
- **Relevant logs / stack trace captured:** `N/A`

### Plan (max 5 steps)
1. Extend `integrations.py` with WhatsApp contracts/stub
2. Extend tests
3. Add docs/spec and JSON stub
4. Run `ruff check .` and `pytest -q`
5. Update PR tracking docs and commit/merge

### Commands I will run locally
- [x] `ruff check .`
- [x] `pytest -q` (or `pytest tests/<specific> -q`)
- [ ] Any smoke command (if relevant): `N/A`

### Safety
- **New deps added?**
  - [x] no
  - [ ] yes — list + reason:
- **Secrets/env touched?**
  - [x] no
  - [ ] yes — explain:

### Exit Criteria
- [ ] CI green on PR
- [ ] Summary + Testing filled in PR body
- [x] No scope creep
