## 🧭 Codex Pre-Flight (Required)

### Persona (pick one)
- [x] persona:core
- [ ] persona:infra
- [ ] persona:docs

### Scope Lock 🔒
- **Goal (one sentence):** Add Slack connector contracts/stubs and docs only (no platform integration implementation) in core.
- **In-scope files:** `packages/core/contracts/integrations.py`, `tests/test_integrations_contracts.py`, `docs/SLACK_CONNECTOR_SPEC.md`, `docs/contracts/stubs/integrations/slack_event.json`, `docs/PR_LOG.md`, `docs/PR_STATE.md`, `docs/pr_notes/PR-111.md`
- **Out-of-scope (3 bullets):**
  1. No real Slack API/webhook integration code
  2. No secrets/token handling or deployment/webhook setup
  3. No platform/client runtime adapters
- **Behavior changes:**
  - [ ] none
  - [x] yes (describe): Adds contracts/docs/stubs for Slack connector integration boundaries only

### Evidence (required)
- **PR description read:** [x]
- **CI failures read (if any):** [x] — N/A
- **Relevant logs / stack trace captured:** `N/A`

### Plan (max 5 steps)
1. Add integrations contract module (Slack section)
2. Add contract tests
3. Add Slack connector spec + JSON stub
4. Run `ruff check .` and `pytest -q`
5. Update PR tracking docs and commit/merge

### Commands I will run locally
- [x] `ruff check .`
- [x] `pytest -q` (or `pytest tests/<specific> -q`)
- [ ] Any smoke command (if relevant): `N/A`

### Safety
- **New deps added?**
  - [x] no
  - [ ] yes — list + reason:
- **Secrets/env touched?**
  - [x] no
  - [ ] yes — explain:

### Exit Criteria
- [ ] CI green on PR
- [ ] Summary + Testing filled in PR body
- [x] No scope creep
