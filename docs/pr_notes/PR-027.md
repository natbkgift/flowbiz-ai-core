## 🧭 Codex Pre-Flight (Required)

### Persona (pick one)
- [x] persona:core
- [ ] persona:infra
- [ ] persona:docs

### Scope Lock 🔒
- **Goal (one sentence):** Add observability hook contracts for trace_id propagation and tool-call logging schema.
- **In-scope files:** `packages/core/contracts/observability.py`, `packages/core/observability.py`, `packages/core/contracts/__init__.py`, `tests/test_observability_hooks.py`, `tests/test_contract_package.py`, `docs/PR_LOG.md`, `docs/PR_STATE.md`, `docs/pr_notes/PR-027.md`
- **Out-of-scope (3 bullets):**
  1. No deploy/infra/VPS/nginx/.github changes
  2. No external logging backend integration
  3. No API route redesign
- **Behavior changes:**
  - [ ] none
  - [x] yes (describe): Introduces reusable observability hook schemas/functions; no endpoint behavior change.

### Evidence (required)
- **PR description read:** [x]
- **CI failures read (if any):** [x]
- **Relevant logs / stack trace captured:** `Baseline on main before branch: ruff check . => PASS, pytest -q => 205 passed`

### Plan (max 5 steps)
1. Add observability contracts for trace and tool-call events
2. Add lightweight helper to create deterministic tool-call log entries
3. Add tests for schemas and helper behavior
4. Run `ruff check .` and `pytest -q`
5. Update PR docs/state and merge ff-only

### Commands I will run locally
- [x] `ruff check .`
- [x] `pytest -q`
- [ ] Any smoke command (if relevant): N/A

### Safety
- **New deps added?**
  - [x] no
  - [ ] yes — list + reason:
- **Secrets/env touched?**
  - [x] no
  - [ ] yes — explain:

### Exit Criteria
- [x] CI green on PR
- [x] Summary + Testing filled in PR body
- [x] No scope creep
