## 🧭 Codex Pre-Flight (Required)

### Persona (pick one)
- [x] persona:core
- [ ] persona:infra
- [ ] persona:docs

### Scope Lock 🔒
- **Goal (one sentence):** Add Local Dev Kit contracts and docs for composing core development services and validation checks without changing deploy/infra files.
- **In-scope files:** `packages/core/contracts/devx.py`, `tests/test_devx_contracts.py`, `docs/LOCAL_DEV_KIT.md`, `docs/PR_LOG.md`, `docs/PR_STATE.md`, `docs/pr_notes/PR-102.md`
- **Out-of-scope (3 bullets):**
  1. No changes to `.github/**`, `docker-compose*.yml`, `nginx/**`, or VPS/system configs
  2. No platform/client integrations or billing/payment implementations
  3. No UI/docs site/playground frontend code
- **Behavior changes:**
  - [ ] none
  - [x] yes (describe): Adds developer-facing contracts/docs for local dev kit planning and validation

### Evidence (required)
- **PR description read:** [x]
- **CI failures read (if any):** [x] — N/A (local roadmap execution)
- **Relevant logs / stack trace captured:** `N/A`

### Plan (max 5 steps)
1. Add `devx` contract models for local dev service definitions and kit plan/validation
2. Add tests for contract defaults/validation
3. Write `docs/LOCAL_DEV_KIT.md` documenting local dev kit composition and scope boundaries
4. Run `ruff check .` and `pytest -q`
5. Update `PR_LOG` + `PR_STATE`, commit/merge `PR-102`

### Commands I will run locally
- [x] `ruff check .`
- [x] `pytest -q` (or `pytest tests/<specific> -q`)
- [ ] Any smoke command (if relevant): `N/A`

### Safety
- **New deps added?**
  - [x] no
  - [ ] yes — list + reason:
- **Secrets/env touched?**
  - [x] no
  - [ ] yes — explain:

### Exit Criteria
- [ ] CI green on PR
- [ ] Summary + Testing filled in PR body
- [x] No scope creep
