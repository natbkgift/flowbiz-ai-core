## 🧭 Codex Pre-Flight (Required)

### Persona (pick one)
- [x] persona:core
- [ ] persona:infra
- [ ] persona:docs

### Scope Lock 🔒
- **Goal (one sentence):** Add email-agent contracts/stubs and docs only in core without SMTP/provider integration implementation.
- **In-scope files:** `packages/core/contracts/integrations.py`, `tests/test_integrations_contracts.py`, `docs/EMAIL_AGENT_SPEC.md`, `docs/contracts/stubs/integrations/email_send_request.json`, `docs/PR_LOG.md`, `docs/PR_STATE.md`, `docs/pr_notes/PR-114.md`
- **Out-of-scope (3 bullets):**
  1. No SMTP/API provider integration
  2. No credentials/secrets/runtime delivery implementation
  3. No infra/deploy changes
- **Behavior changes:**
  - [ ] none
  - [x] yes (describe): Adds email agent contracts/stubs/docs only

### Evidence (required)
- **PR description read:** [x]
- **CI failures read (if any):** [x] — N/A
- **Relevant logs / stack trace captured:** `N/A`

### Plan (max 5 steps)
1. Extend `integrations.py` with email agent contracts/stub
2. Extend tests
3. Add docs/spec and JSON stub
4. Run `ruff check .` and `pytest -q`
5. Update PR tracking docs and commit/merge

### Commands I will run locally
- [x] `ruff check .`
- [x] `pytest -q` (or `pytest tests/<specific> -q`)
- [ ] Any smoke command (if relevant): `N/A`

### Safety
- **New deps added?**
  - [x] no
  - [ ] yes — list + reason:
- **Secrets/env touched?**
  - [x] no
  - [ ] yes — explain:

### Exit Criteria
- [ ] CI green on PR
- [ ] Summary + Testing filled in PR body
- [x] No scope creep
