## 🧭 Codex Pre-Flight (Required)

### Persona (pick one)
- [ ] persona:core
- [ ] persona:infra
- [x] persona:docs

### Scope Lock 🔒
- **Goal (one sentence):** Document docs-site architecture/spec for FlowBiz AI Core content publishing without implementing any frontend/docs-site code in this repo.
- **In-scope files:** `docs/DOCS_SITE_SPEC.md`, `docs/PR_LOG.md`, `docs/PR_STATE.md`, `docs/pr_notes/PR-106.md`
- **Out-of-scope (3 bullets):**
  1. No frontend/docs-site code (Next.js/Vite/React/etc.)
  2. No CI/deploy/hosting changes
  3. No API/runtime behavior changes
- **Behavior changes:**
  - [ ] none
  - [x] yes (describe): Adds design/spec documentation only for out-of-scope docs site implementation

### Evidence (required)
- **PR description read:** [x]
- **CI failures read (if any):** [x] — N/A
- **Relevant logs / stack trace captured:** `N/A`

### Plan (max 5 steps)
1. Add docs-site spec doc with scope boundaries and ownership
2. Update PR tracking docs
3. Run `ruff check .` and `pytest -q` (baseline unchanged expected)
4. Commit `PR-106`
5. Merge ff-only to `main`

### Commands I will run locally
- [x] `ruff check .`
- [x] `pytest -q` (or `pytest tests/<specific> -q`)
- [ ] Any smoke command (if relevant): `N/A`

### Safety
- **New deps added?**
  - [x] no
  - [ ] yes — list + reason:
- **Secrets/env touched?**
  - [x] no
  - [ ] yes — explain:

### Exit Criteria
- [ ] CI green on PR
- [ ] Summary + Testing filled in PR body
- [x] No scope creep
