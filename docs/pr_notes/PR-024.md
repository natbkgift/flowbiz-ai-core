## 🧭 Codex Pre-Flight (Required)

### Persona (pick one)
- [x] persona:core
- [ ] persona:infra
- [ ] persona:docs

### Scope Lock 🔒
- **Goal (one sentence):** Implement Agent Registry v2 with register/enable/disable lifecycle and integrate it into the core runtime without platform-specific logic.
- **In-scope files:** `packages/core/contracts/agent_registry.py`, `packages/core/agent_registry.py`, `packages/core/runtime/runtime.py`, `packages/core/contracts/__init__.py`, `tests/test_agent_registry.py`, `tests/test_runtime_unit.py`, `docs/PR_LOG.md`, `docs/PR_STATE.md`, `docs/pr_notes/PR-024.md`
- **Out-of-scope (3 bullets):**
  1. No changes to infra/deploy/VPS/nginx/compose/.github files
  2. No API route behavior redesign or new endpoints
  3. No platform/client adapters or external integrations
- **Behavior changes:**
  - [ ] none
  - [x] yes (describe): Runtime gains explicit enable/disable semantics via Agent Registry v2 while preserving existing default echo behavior.

### Evidence (required)
- **PR description read:** [x]
- **CI failures read (if any):** [x]
- **Relevant logs / stack trace captured:** `Baseline on main before branch: ruff check . => PASS, pytest -q => 192 passed`

### Plan (max 5 steps)
1. Add schema-only agent registry contracts
2. Implement in-memory deterministic Agent Registry v2
3. Integrate registry into `AgentRuntime` with enable/disable checks
4. Add/adjust tests for registry and runtime behavior
5. Run quality checks and update PR docs/state

### Commands I will run locally
- [x] `ruff check .`
- [x] `pytest -q`
- [ ] Any smoke command (if relevant): N/A

### Safety
- **New deps added?**
  - [x] no
  - [ ] yes — list + reason:
- **Secrets/env touched?**
  - [x] no
  - [ ] yes — explain:

### Exit Criteria
- [x] CI green on PR
- [x] Summary + Testing filled in PR body
- [x] No scope creep
