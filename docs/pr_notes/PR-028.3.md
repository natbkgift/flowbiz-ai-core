## 🧭 Codex Pre-Flight (Required)

### Persona (pick one)
- [x] persona:core
- [ ] persona:infra
- [ ] persona:docs

### Scope Lock 🔒
- **Goal (one sentence):** Add deterministic prompt versioning primitives on top of the prompt template system.
- **In-scope files:** `packages/core/contracts/prompt_template.py`, `packages/core/prompt_templates.py`, `tests/test_prompt_templates.py`, `docs/pr_notes/PR-028.3.md`, `docs/PR_LOG.md`, `docs/PR_STATE.md`
- **Out-of-scope (3 bullets):**
  1. No provider/runtime routing integration
  2. No infra/deploy/VPS/nginx/.github changes
  3. No client/platform-specific prompt rules
- **Behavior changes:**
  - [ ] none
  - [x] yes (describe): Prompt registry now supports versioned templates and rendering specific versions.

### Evidence (required)
- **PR description read:** [x]
- **CI failures read (if any):** [x]
- **Relevant logs / stack trace captured:** `Baseline on main before branch: ruff check . => PASS, pytest -q => 221 passed`

### Plan (max 5 steps)
1. Extend prompt template contracts with version metadata
2. Add version-aware registration and render methods
3. Add tests for default-latest and explicit-version rendering
4. Run `ruff check .` and `pytest -q`
5. Update PR docs/state and merge ff-only

### Commands I will run locally
- [x] `ruff check .`
- [x] `pytest -q`
- [ ] Any smoke command (if relevant): N/A

### Safety
- **New deps added?**
  - [x] no
  - [ ] yes — list + reason:
- **Secrets/env touched?**
  - [x] no
  - [ ] yes — explain:

### Exit Criteria
- [x] CI green on PR
- [x] Summary + Testing filled in PR body
- [x] No scope creep
