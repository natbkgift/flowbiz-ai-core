## 🧭 Codex Pre-Flight (Required)

### Persona (pick one)
- [x] persona:core
- [ ] persona:infra
- [ ] persona:docs

### Scope Lock 🔒
- **Goal (one sentence):** Add deterministic example agents for developer reference and tests in core without platform integrations.
- **In-scope files:** `packages/core/agents/examples.py`, `tests/test_example_agents.py`, `docs/EXAMPLE_AGENTS.md`, `docs/PR_LOG.md`, `docs/PR_STATE.md`, `docs/pr_notes/PR-104.md`
- **Out-of-scope (3 bullets):**
  1. No platform/client-specific agent integrations (Slack, LINE, etc.)
  2. No changes to infra/deploy files (`.github`, compose, nginx, VPS)
  3. No API endpoint changes beyond existing runtime contracts
- **Behavior changes:**
  - [ ] none
  - [x] yes (describe): Adds reusable deterministic example agents and tests for developer guidance

### Evidence (required)
- **PR description read:** [x]
- **CI failures read (if any):** [x] — N/A
- **Relevant logs / stack trace captured:** `N/A`

### Plan (max 5 steps)
1. Add example agent implementations in `packages/core/agents/examples.py`
2. Add tests covering deterministic outputs and traces
3. Add docs for usage/examples and scope constraints
4. Run `ruff check .` and `pytest -q`
5. Update `PR_LOG` + `PR_STATE`, commit/merge `PR-104`

### Commands I will run locally
- [x] `ruff check .`
- [x] `pytest -q` (or `pytest tests/<specific> -q`)
- [ ] Any smoke command (if relevant): `N/A`

### Safety
- **New deps added?**
  - [x] no
  - [ ] yes — list + reason:
- **Secrets/env touched?**
  - [x] no
  - [ ] yes — explain:

### Exit Criteria
- [ ] CI green on PR
- [ ] Summary + Testing filled in PR body
- [x] No scope creep
