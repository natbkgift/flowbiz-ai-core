## 🧭 Codex Pre-Flight (Required)

### Persona (pick one)
- [x] persona:core
- [ ] persona:infra
- [ ] persona:docs

### Scope Lock 🔒
- **Goal (one sentence):** Add SDK generator specification contracts and examples/docs without implementing language-specific generator runtimes in core.
- **In-scope files:** `packages/core/contracts/devx.py`, `tests/test_devx_contracts.py`, `docs/SDK_GENERATORS.md`, `docs/contracts/stubs/sdk_generators/*`, `docs/PR_LOG.md`, `docs/PR_STATE.md`, `docs/pr_notes/PR-108.md`
- **Out-of-scope (3 bullets):**
  1. No actual SDK code generation engines or language toolchains
  2. No publishing pipelines/package registry automation
  3. No UI/deploy/infra changes
- **Behavior changes:**
  - [ ] none
  - [x] yes (describe): Adds contracts and examples for SDK generator specs only

### Evidence (required)
- **PR description read:** [x]
- **CI failures read (if any):** [x] — N/A
- **Relevant logs / stack trace captured:** `N/A`

### Plan (max 5 steps)
1. Extend `devx` contracts with SDK generator spec models/helpers
2. Extend tests for validation and target selection
3. Add docs and example generator spec stubs
4. Run `ruff check .` and `pytest -q`
5. Update `PR_LOG` + `PR_STATE`, commit/merge `PR-108`

### Commands I will run locally
- [x] `ruff check .`
- [x] `pytest -q` (or `pytest tests/<specific> -q`)
- [ ] Any smoke command (if relevant): `N/A`

### Safety
- **New deps added?**
  - [x] no
  - [ ] yes — list + reason:
- **Secrets/env touched?**
  - [x] no
  - [ ] yes — explain:

### Exit Criteria
- [ ] CI green on PR
- [ ] Summary + Testing filled in PR body
- [x] No scope creep
